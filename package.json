{
  "scripts": {
    "dev": "wrangler dev src/index.ts  --live-reload",
    "deploy": "wrangler deploy --minify src/index.ts",
    "build": "wrangler deploy --outdir=./dist --minify src/index.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.1.11",
    "hono": "^3.10.2",
    "htmx.org": "^1.9.9",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230914.0",
    "jspdf": "^2.5.1",
    "wrangler": "^3.18.0"
  }
}
